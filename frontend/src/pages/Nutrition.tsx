import { Apple } from 'lucide-react';
import ModulePlaceholder from '../components/ModulePlaceholder';

export default function Nutrition() {
  return (
    <ModulePlaceholder
      icon={Apple}
      title="Nutrition"
      description="Lightweight meal logging — text-first, optional protein and calorie estimates."
    />
  );
}
